import classes from "./CreatedMovielists.module.css";

const CreatedMovielist = () => {
    return(
        <div className={classes.pContainer}>
            <h1 className={classes.title}>CREATED MOVIELISTS</h1>
        </div>
    );
}

export default CreatedMovielist;